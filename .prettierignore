dist
coverage
node_modules
package-lock.json

# authored documents — preserved verbatim
docs/mvp-scope.md

# Recorded ADS-B fixture, written by scripts/capture-adsb.ts — one frame per line so a recapture
# diffs as data. Prettier would expand it to ~37,000 lines and 1.7x the bytes, destroying that.
public/*.json

# Golden inject scenario, written by scripts/generate-inject-golden.ts — one track per line so a
# regeneration diffs as data. Never hand-edit; run `npm run fixture:injects`.
src/lib/__fixtures__/*.json

# The bench baseline, written by scripts/bench.ts — a generated table Prettier would re-align.
# Never hand-edit; run `npm run bench:baseline`.
docs/bench/baseline.md

# The study replay tool’s fixtures — the run JSON as Copy run put it on the clipboard, byte for
# byte (S5a): one event per line. Never hand-edit; captured from a headless run.
tools/replay/__fixtures__/*.json

# The demo scenario's two runs (S11b, #214), captured the same way; the folder walk of --study never sees a subfolder.
tools/replay/__fixtures__/demo/*.json
